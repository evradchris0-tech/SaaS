{
  "name": "nkap-monorepo",
  "private": true,
  "description": "Nkap — plateforme multi-tontines (monorepo : docs/ + nkap-core-service/). Outillage qualité au niveau racine.",
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write \"**/*.{ts,js,json,md,yml,yaml}\""
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.4.2"
  }
}
